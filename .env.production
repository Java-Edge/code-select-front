## 生产环境

# 变量必须以 VUE_APP_ 为前缀才能暴露给外部读取

# 注意：不要在此声明 NODE_ENV —— Vue CLI 执行 build 时会自动置为 'production'，
# 手动覆盖会打断依赖该标志的三方库 tree-shaking 与分支优化。

# API 请求前缀
# 必须与 src/axios.js 中 baseURL 的兜底值保持一致：
#   baseURL: process.env.VUE_APP_BASE_API || '/api'
# 生产环境由 nginx 将 /api 反代到后端，故此处同为 '/api'。
#
# 历史问题：仓库曾只有 .env.development，生产构建时该变量为 undefined，
# 已删除的 utils/request.js 实例 baseURL 落空，导致请求丢失 /api 前缀被
# nginx 落进 location / 并由 try_files 返回 index.html。
# 现由本文件显式声明，使生产构建不再依赖代码内的字符串兜底。
VUE_APP_BASE_API = '/api'
